<% content_for :body_class, "page-body" %>
<div class="page-container">
  <div class="sidebar-menu">
    <%=render :partial => "layouts/dashboard_menuheader" %>
    <%=render :partial => "layouts/dashboard_menu" %>
  </div>
  <div class="main-content">
    <%=render :partial => "layouts/dashboard_topbar" %>
    <ol class="breadcrumb bc-3">
      <li>
        <a href="<%=url_for :controller => "dashboard", :action => "index" %>"><i class="entypo-home"></i>Dashboard</a>
      </li>
      <li class="active">
        <strong>Restaurant</strong>
      </li>
    </ol>
    <div class="profile-env">
     	<header class="row" style="height:100px;">
     		<div class="col-sm-2">
     			<img src="<%=@user.logo_image.url(:cropped_website) %>" alt="" class="" width="108" height="72" />
     		</div>
     		<div class="col-sm-10">
     			<ul class="profile-info-sections">
     				<li>
     					<div class="profile-name">
     						<strong style="color:#000">
     							<%=@user.restaurant.name %>
                </strong>
     						<span>
     							<%=@user.email %>
     						</span>
     					</div>
     				</li>
     				<li>
     					<div class="profile-stat">
     						<h3><%=@user.devices.actives.count %></h3>
     						<span>iPads</span>
     					</div>
     				</li>
     				<li>
     					<div class="profile-stat">
     						<h3><%=@user.restaurant.menus.count %></h3>
     						<span><a href="<%=url_for :controller => "menumalist", :action => "index" %>">Menus</a></span>
     					</div>
     				</li>
     				<li>
     					<div class="profile-stat">
     						<h3><%=@user.restaurant.dishes.count %></h3>
     						<span>Dishes</span>
     					</div>
     				</li>
     			</ul>
     		</div>
     	</header>
      <div class="row">
          <div class="col-sm-12">
            <div class="profile-buttons">
            	<a href="javascript:;" onclick="jQuery('#modal-editsettings').modal('show');" class="btn btn-default">
            		<i class="entypo-cog"></i>
            		Account settings
            	</a>
            	<a href="javascript:;" onclick="jQuery('#modal-editdescription').modal('show');" class="btn btn-default">
            		<i class="entypo-brush"></i>
            		Restaurant description
            	</a>
            </div>
          </div>
        </div>
     	<header class="row" style="margin-bottom:60px;">
     		<img src="<%=@user.restaurant_image.url(:cropped_default_retina) %>" alt="" class="col-sm-4" />
     		<div class="col-sm-8">
          <br />
          <%=@user.restaurant.description %>
     		</div>
     	</header>
     	<section class="profile-info-tabs">
     		<div class="row">
     			<div class="col-sm-offset-2 col-sm-10">
     				<ul class="user-details">
              <% if @user.restaurant.categories.count >= 1 %>
     					<li>
                <%=@user.restaurant.categories.first.title %>
     					</li>
              <% end %>
              <% if @user.restaurant.categories.count == 2 %>
     					<li>
                <%=@user.restaurant.categories.last.title %>
     					</li>
              <% end %>
     				</ul>
     			</div>
     		</div>
     	</section>
    </div>
  </div>
</div>
<%=render :partial => "modal_editsettings" %>
<%=render :partial => "modal_editdescription" %>