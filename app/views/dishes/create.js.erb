$('#default-modal').modal('hide');
var new_category = $("<%= j render partial: 'dish', locals: {category: @dish} %>");
$(new_category).hide().appendTo("#form_sort_dish .ui-sortable").fadeIn(1500);

hide_new_tile("category");