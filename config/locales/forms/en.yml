en:
  forms:
    new: "Add %{model}"
    submit_new: "Add"
    create: "Add %{model}"
    submit_create: "Add"
    edit: "Update %{model}"
    submit_edit: "Update"
    update: "Update %{model}"
    submit_update: "Update"
    cancel: "cancel"
    upload_image: "Upload image"
    submit: "Submit"
    
    # destroy_image renders edit, so it must say the same
    destroy_image: "Update %{model}"
    submit_destroy_image: "Update"